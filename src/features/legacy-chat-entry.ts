/** Cordis row wrapper plus the pure required legacy Chat factory. */
export {
  apply,
  inject,
  legacyChatFeature,
  name,
  provide,
  type DshTuiLegacyChatMount,
} from '../composition/legacy-chat-plugin.ts'
