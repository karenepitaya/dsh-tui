# Source provenance

This independent MIT package was copied on 2026-09-07 from the user's local
pi-tui-orbs component library, previously kept beside dsh-tui. The original
directory is retained. No upstream repository URL, tag, or commit is claimed.
See LICENSE for the original copyright and permission notice.

The imported baseline contains 60 files: src/, test/, examples/, scripts/,
.gitignore, LICENSE, README.md, package.json, all three tsconfig files, and
vitest.config.ts. Generated output, node_modules, private artifacts, and the
old standalone lockfile are excluded; the repository root pnpm-lock.yaml now
locks both workspace packages.

Baseline SHA-256: 39d4546903d0ac1f8bee537485e28b5f9e60972640ce623bdbc227943c341cf7

To reproduce the digest, sort baseline relative paths by Unicode code point,
use forward slashes, hash each file's original bytes with SHA-256, then hash
the UTF-8 concatenation of path + NUL + lowercase file hash + LF. SOURCE.md
and later additions are not part of this imported baseline.
